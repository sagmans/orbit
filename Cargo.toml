[package]
name = "orbit"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Run coding-agent CLIs inside hardened containers with host-mount isolation"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
signal-hook = "0.3"

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"
